@page "/Informe/Crear/{cliente}"
@attribute [Authorize]
<AuthorizeView>
    <Authorized>
        <CrearInforme Cliente="@cliente"></CrearInforme>
    </Authorized>
</AuthorizeView>

@code {
    [Parameter]
    public string cliente { get; set; } = string.Empty;
}
